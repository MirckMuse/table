export interface IViewport {
    width: number;
    height: number;
    scrollWidth: number;
    scrollHeight: number;
}
export declare class Viewport implements IViewport {
    width: number;
    height: number;
    scrollWidth: number;
    scrollHeight: number;
}
